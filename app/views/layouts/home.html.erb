<!DOCTYPE html>
<html>
    <%= render 'layouts/head' %>

    <body>
        <div class="container-fluid">
        
            <%= render 'layouts/navbar' %>

            <div class="row">
                <%= yield %>
            </div>
        </div>
    </body>
</html>
